spring:
  datasource:
    url: jdbc:postgresql://postgres-order:5432/order_db
    username: postgres
    password: postgres

jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-must-be-at-least-32-characters-long-for-security}

domain:
  monthly-price: ${DOMAIN_MONTHLY_PRICE:200}
  yearly-discount: ${DOMAIN_YEARLY_DISCOUNT:0.7}
