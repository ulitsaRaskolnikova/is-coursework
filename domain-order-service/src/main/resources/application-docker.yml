spring:
  datasource:
    url: jdbc:postgresql://postgres-domain-order:5432/domain_order_db
    username: postgres
    password: postgres

jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-must-be-at-least-32-characters-long-for-security}

exdns:
  url: ${EXDNS_URL:http://exdns:8080}
  api-token: ${EXDNS_API_TOKEN:changeme}
